# Codex Adapters

Place Codex-specific glue code here when bridging shared automation (e.g., hooking CLI commands into Codex workflows). Reference `.spec-flow/` docs and `.claude/` behavior before adding new adapters. Other tools treat this folder as read-only.***
